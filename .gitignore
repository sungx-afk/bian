.DS_Store
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
/test/unit/coverage/
/test/e2e/reports/
selenium-debug.log

/dist/

# ===== Capacitor Web 资源：由 CI 从 /dist 生成，不入库 =====
# 同步方式：根目录 npm run build → ios-app 下 npm run copy:web
/ios-app/www/

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln

# ===== 签名证书与密钥：一律禁止提交 =====
# 公开仓库里的 p8/证书会被自动扫描盗用，只能去 Apple 后台吊销重签
*.p8
AuthKey_*.p8
*.p12
*.mobileprovision
*.cer
*.certSigningRequest
*.provisionprofile
private_keys/
# 例外：CI 签名用的分发证书与 App Store 描述文件需要入库。
# 二者都是公开数据，不含私钥（私钥只存在于 SIGNING_PRIVATE_KEY Secret），
# 放进仓库是为了免去手工 base64，纯网页操作即可完成配置。
!ios-app/ci/apple-distribution.cer
!ios-app/ci/bian-appstore.mobileprovision
ios-app/ExportOptions.plist
ios-app/*.ipa
ios-app/build/
